{
  "name": "{{.Title}}",
  "short_name": "{{.Title}}",
  "description": "{{.Params.description}}",
  "start_url": "/",
  "icons": [
    { 
      "size": "512x512",
      "src": "{{.Params.image}}"
    }
  ]
}